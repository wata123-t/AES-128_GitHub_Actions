//-----------------------------------------------------------------------------------
>docker-compose up -d

>docker-compose exec verilog bash
→このコマンドで、コンテナ内に入る
///////////////////////////////////////////////////
// コンテナ内　で実行
///////////////////////////////////////////////////
>apt-get update && apt-get install -y iverilog gtkwave

# 全てのVerilogファイルをまとめてコンパイル
>iverilog -o aes_sim *.v
# シミュレーション実行（波形ファイル aes_test.vcd が生成される）
>vvp aes_sim


///////////////////////////////////////////////////
// Docker終了コマンド(コンテナから出てから実行)
///////////////////////////////////////////////////

docker-compose down
docker system prune

//-----------------------------------------------------------------------------------
