services:
  # Python, Verilog の実行
  aes-verify:
    build:
      context: .
      dockerfile: ./Dockerfile
    volumes:
      - .:/app
    working_dir: /app

  # ★ dbt 実行
  dbt:
    image: ghcr.io/dbt-labs/dbt-bigquery:latest
    volumes:
      - .:/app
    environment:
      - DBT_PROFILES_DIR=/app/aes_dbt
      - GOOGLE_APPLICATION_CREDENTIALS=/app/python/credentials/bq-writer.json
    working_dir: /app/aes_dbt
    
    